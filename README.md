# turbogrep


```
## Add to your .bashrc
export VOYAGE_API_KEY=''
export TURBOPUFFER_API_KEY=""

# Clone and install
git clone git@github.com:turbopuffer/turbogrep.git
cargo install --path .

# Try it!
cd ~/turbopuffer
tg --verbose 'grouping similar vectors together'
```
